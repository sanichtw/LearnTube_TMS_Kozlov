<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Установка фонового цвета -->
        <item name="android:windowBackground">@android:color/white</item>
        <!-- Установка градиента для кнопок -->
    </style>

</resources>

